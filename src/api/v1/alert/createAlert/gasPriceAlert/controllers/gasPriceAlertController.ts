import { NextFunction, Request, Response } from "express";

async function GasPriceAlertController(
  req: Request,
  res: Response,
  next: NextFunction,
) {
  try {
    
  } catch (error) {
    next(error);
  }
}
